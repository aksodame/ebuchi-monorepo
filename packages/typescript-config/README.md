# `@ebuchi-bot/typescript-config`

- `base.json` — NodeNext module + strict + declaration emit, base for all packages
- `nextjs.json` — extends `base`, ESNext + Bundler + JSX preserve, noEmit for Next.js apps
- `nestjs.json` — extends `base`, ESNext + Bundler + decorators + emitDecoratorMetadata for NestJS
