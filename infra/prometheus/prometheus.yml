global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: nestjs-server
    static_configs:
      - targets:
          - host.docker.internal:8080
    metrics_path: /metrics
    authorization:
      credentials_file: /tmp/internal-api-key
